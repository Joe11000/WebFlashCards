<div class="container">
  <h1>Select a Deck</h1>

  <ul>
  <% if @decks %>
    <% @decks.each do |deck| %>
      <li><a href="/<%= @user_name%>/decks/<%= deck.name %>/create_round"><%= deck.name %></a></li>
    <% end %>
  <% end %>
  </ul>
</div>
